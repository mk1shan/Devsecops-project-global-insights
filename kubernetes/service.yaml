apiVersion: v1
kind: Service
metadata:
  name: global-details
  labels:
    app: global-details
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: global-details
